# Learn more about services, parameters and containers at
# http://symfony.com/doc/current/book/service_container.html
parameters:
#    parameter_name: value

services:
    documents.storer.configuration:
        class:    AppBundle\Service\DocumentStorer\GoogleApiCredentialsConfiguration
        arguments: ['@logger']

    documents.storer:
        class:     AppBundle\Service\DocumentStorer\GoogleApiDocumentStorer
        arguments: ['@documents.storer.configuration', '%shared.folder.id%', '@logger']